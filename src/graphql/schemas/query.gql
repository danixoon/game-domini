input GiftFilter {
  after: Date
  before: Date
  authorIds: [ID!]
}

type Query {
  players: [Player!]!
  player(id: ID!): Player
  gifts(filter: GiftFilter!): [Gift!]!
  resources(id: ID!): [Resource!]!
}
